```c++
int i = -1, &r = 0; // INVALID: cannot bind a non-const reference (&r) to an integer literal (0)
int *const p2 = &i2; // INVALID: i2 is not declared
const int i = -1, &r = 0; // VALID: &r is a const reference and can bind to an integer literal
const int *const p3 = &i2; // INVALID: i2 is not declared
const int *p1 = &i2; // INVALID: i2 is not declared
const int &const r2; // INVALID: references cannot be const themselves
const int i2 = i, &r = i; // VALID: const reference can bind to non-const reference
```