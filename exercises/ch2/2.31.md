```c++
r1 = v2; // LEGAL: top level const; const in v2 is ignored

p1 = p2; // ILLEGAL: low level; cannot modify p1 (int*) 
p2 = p1; // LEGAL: high level

p1 = p3; // ILLEGAL: low level; p3 is a (const *), cannot be modified by p1
p2 = p3; // LEGAL: copies the address of p3 but not modify p3 itself
```